
#define COMMENT(hash, name) hash include name
COMMENT(#,"hello")

#if 0
Define a base image and provide a tag for it, default is latest
#endif
FROM ubuntu:latest
MAINTAINER me

#include "../buildsteps/apt-get.start.txt"
#include "../buildsteps/apt-get.install.devtools.txt"
#include "../buildsteps/apt-get.stop.txt"